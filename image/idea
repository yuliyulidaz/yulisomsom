<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Yulipomo Midnight Warning Generator</title>
    <style>
        /* 1. 프리텐다드 폰트 불러오기 (웹폰트) */
        @import url("https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css");
        /* 영문/숫자용 코딩 폰트도 유지 */
        @import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;800&display=swap');

        body {
            margin: 0; padding: 0;
            background-color: #333;
            display: flex; justify-content: center; align-items: center;
            height: 100vh;
            /* 2. 폰트 적용 순서: 프리텐다드 우선 */
            font-family: 'Pretendard', 'JetBrains Mono', monospace;
            /* 3. 한글 줄바꿈 및 자간 최적화 */
            word-break: keep-all; 
        }

        /* 메인 컨테이너 */
        .dark-container {
            width: 1200px; height: 600px;
            position: relative; overflow: hidden;
            background-color: #121212;
            background-image: 
                linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
            background-size: 50px 50px;
            display: flex; justify-content: center; align-items: center;
            box-shadow: 0 0 60px rgba(0,0,0,0.9);
        }

        /* 중앙 글로우 */
        .glow-spot {
            position: absolute;
            width: 800px; height: 800px;
            background: radial-gradient(circle, rgba(255, 120, 50, 0.18) 0%, rgba(0,0,0,0) 65%);
            top: 50%; left: 50%; transform: translate(-50%, -30%); z-index: 0;
        }

        /* 상단 텍스트 그룹 */
        .text-group {
            position: absolute; top: 60px; left: 60px; z-index: 20;
        }
        .main-title {
            font-size: 36px; /* 한글이라 폰트 사이즈 살짝 키움 */
            font-weight: 800; 
            color: #fff; 
            letter-spacing: -1.5px; /* 제목 자간 좁게 */
            margin-bottom: 12px;
            line-height: 1.2;
        }
        .sub-title {
            font-size: 17px; 
            color: #ff9f43; 
            display: flex; align-items: center; gap: 10px;
            font-weight: 500;
            letter-spacing: -0.5px; /* 본문 자간 조정 */
        }
        .cursor {
            width: 10px; height: 18px; background-color: #ff9f43; animation: blink 1s step-end infinite;
        }
        @keyframes blink { 50% { opacity: 0; } }

        /* --- 추가된 경고문 스타일 --- */
        .warning-box {
            position: absolute;
            bottom: 40px; 
            left: 50%;
            transform: translateX(-50%); 
            width: 70%; 
            padding: 18px 25px; /* 여백 살짝 조정 */
            background: rgba(15, 15, 15, 0.85); 
            border: 1px solid #ff4757; 
            border-left: 6px solid #ff4757; 
            border-radius: 6px;
            color: #eee; 
            font-size: 15px; /* 가독성 위해 1px 키움 */
            line-height: 1.5; /* 줄간격 확보 */
            display: flex; align-items: center; gap: 15px;
            z-index: 50; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.6);
            backdrop-filter: blur(5px);
            letter-spacing: -0.5px; /* 한글 자간 */
        }
        .warning-icon { font-size: 24px; }
        .warning-tag { color: #ff4757; font-weight: 800; margin-right: 5px; white-space: nowrap; }


        /* 폰 배치 래퍼 */
        .phones-wrapper {
            display: flex; justify-content: center; align-items: center;
            perspective: 1200px; position: relative; top: 40px; width: 100%; height: 100%;
        }
        /* 폰 공통 스타일 */
        .phone-frame {
            width: 240px; height: 480px; background: #000; border-radius: 32px; border: 4px solid #333;
            box-shadow: 0 30px 60px rgba(0,0,0,0.8); position: absolute; overflow: hidden; transition: all 0.5s ease;
        }
        .screen-img { width: 100%; height: 100%; background-size: cover; background-position: center 12%; }

        /* 폰 위치 설정 */
        .p1 { transform: translateX(-380px) translateZ(-200px) rotateY(25deg); opacity: 0.5; z-index: 1; }
        .p5 { transform: translateX(380px) translateZ(-200px) rotateY(-25deg); opacity: 0.5; z-index: 1; }
        .p2 { transform: translateX(-180px) translateZ(-100px) rotateY(15deg); opacity: 0.8; z-index: 5; }
        .p4 { transform: translateX(180px) translateZ(-100px) rotateY(-15deg); opacity: 0.8; z-index: 5; }
        .p3 { width: 270px; height: 540px; transform: translateZ(0px); z-index: 10; border-color: #ff9f43; box-shadow: 0 0 40px rgba(255, 159, 67, 0.4), 0 50px 100px rgba(0,0,0,0.9); opacity: 1; }

    </style>
</head>
<body>
    <div class="dark-container">
        <div class="glow-spot"></div>

        <div class="text-group">
            <div class="main-title">최애 뽀모도로</div>
            <div class="sub-title">최애야, 이제는 네가 내 현생 좀 응원해 줘... 응?<div class="cursor"></div></div>
        </div>

        <div class="warning-box">
            <span class="warning-icon">⏰</span>
            <div>
                <span class="warning-tag">[뽀모도로]</span>
                집중 25분 + 휴식 5분을 4번 반복하여 집중력을 관리하는 시간관리 기법입니다.
            </div>
        </div>
        
        <div class="phones-wrapper">
            <div class="phone-frame p1"><div class="screen-img" style="C:/pomodoro/스샷/1.jpg"></div></div>
            <div class="phone-frame p2"><div class="screen-img" style="background-image: url('YOUR_IMAGE_2.jpg');"></div></div>
            <div class="phone-frame p3"><div class="screen-img" style="background-image: url('YOUR_IMAGE_MAIN.jpg');"></div></div>
            <div class="phone-frame p4"><div class="screen-img" style="background-image: url('YOUR_IMAGE_4.jpg');"></div></div>
            <div class="phone-frame p5"><div class="screen-img" style="background-image: url('YOUR_IMAGE_5.jpg');"></div></div>
        </div>

    </div>
</body>
</html>
